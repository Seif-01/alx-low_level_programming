#include <stdio.h>

/**
 * main - entry point
 *
 * Description: prints a specific message
 *
 * Return: always 0
 *
 */

int main(void)
{
        printf("with proper grammar, but the outcome is a piece of art,");
        return (0);
}
